# 変更履歴

## 0.3.0 (未リリース)

登録した人のコメントに反応してリンクを投稿できるようにした (specs/004-comment-link)。

### 機能

- **引き金が 2 種類になった。**これまでの「リダイレクトを受けた」に加えて、
  **辞書で「コメントに反応する」を付けた人がチャットにコメントした**ときにも投稿する。
  **既定は OFF**(設定のスイッチと辞書のフラグの両方。何もしなければ 0.2.0 までと同じ挙動)
- コメント返し専用の文面 `commentTemplate` と、**コメント返し専用の自由文**
  (リダイレクト返礼の `{msg}` とは別のフィールド)
- **コメント返しは固定しない。**固定枠は 1 件しかなく、リダイレクト返礼の固定が流れるため
- 頻度の上限: **同じ配信で同じ人に 1 回** / 最低 5 秒間隔 / **1 配信 20 件**
- 抑止は非対称 — リダイレクト返礼を投稿済みならコメント返しはしないが、
  コメント返し済みでもリダイレクト返礼は投稿する(こちらが本命)
- 設定画面: コメント返しのセクション、**辞書テーブルを行ごとに畳む表示**、
  「設定したのに効かない行」の印、投稿履歴の種別列

### 変更

- **拡張の表示名を「リダイレクト返礼ピン」から「返礼リンク」に変えた。**
  引き金がリダイレクト以外にも増え、コメント返しでは固定もしないため、
  旧名は機能の半分しか指していなかった。**変えたのは表示名だけ**で、
  リポジトリ名・ZIP 名・ログの `[yt-redirect-pin]` はそのまま。
  `key` があるので**拡張 ID は変わらず、設定は消えない**
  → **その後、同じ 0.3.0 のうちにプロダクトコードも改名した(次の項)。**
- **プロダクトコードを `yt-redirect-pin` から `shoutlink` に変えた。**
  リポジトリ名(`max-enterme/shoutlink`)・パッケージ名・ZIP 名・
  manifest の表示名の括弧内(`返礼リンク (shoutlink)`)・
  コンソールログの接頭辞(`[shoutlink]`)・手動トリガーの DOM id(`#shoutlink-manual-trigger`)。
  **表示名「返礼リンク」は変えていない。**
  - **`chrome.storage` のキー(`ytRedirectPin.*`)は変えていない。**
    内部名で利用者に見えず、**変えると既存の設定・辞書・投稿履歴が読めなくなる**ため。
  - ⚠️ **説明ページの URL が変わった** → <https://max-enterme.github.io/shoutlink/>。
    **GitHub Pages は旧 URL をリダイレクトしない(404 になる)。**
    リポジトリの URL は 301 で新しい名前へ追従する(blob URL も含む)。
  - `docs/t1-findings.md` の旧名は**観測記録なので書き換えていない。**
- **`manifest.json` の `key` を、Chrome ウェブストアが割り当てた公開鍵に差し替えた。**
  **拡張 ID が `bfmfamnekclamfdjbfndmomljgneejgo` から
  `kpjfmcppkncdmehdpgcochaanbmodaim` に変わる**(ストアのアイテム ID と同じ)。
  - ⚠️ **デベロッパーモードで入れている場合、この版を読み込むと別の拡張として扱われ、
    設定・呼び名の辞書・投稿履歴を引き継がない。**
    入れ替える前に設定ページの DevTools で `chrome.storage` を書き出しておくこと
    (手順 → [docs/setup-and-verify.md](docs/setup-and-verify.md) 「ストアへ移すと拡張 ID が変わる」)。
  - **揃えた理由は二重投稿の防止。** ID が違うとストア版と並存できてしまい、
    **両方が動いて同じ相手へ 2 回投稿する。**揃えれば Chrome の重複防止が効く。
  - ストア提出用 ZIP は今までどおり `key` を落とすので、`npm run package:store` の出力は変わらない。
- **`host_permissions` に `https://www.youtube.com/*` が増えた。**
  **インストール時の許可表示が変わる。**用途は**設定画面から 1 回チャンネル ID を取りに行く**ことだけで、
  **注入範囲は増えていない**(content script は `studio.youtube.com/live_chat*` のまま)。
  **ライブチャットの画面では通信しない**
- 辞書に `channelId` を持たせた。コメントの照合は**チャンネル ID の一致だけ**で行い、
  **表示名は使わない**(同名の別人・改名・なりすましで誤爆しないため)
- **これ以降、版を入れ替えても設定・呼び名の辞書・投稿履歴が引き継がれる。**
  `manifest.json` に固定の `key` を置き、**フォルダの場所に依らず同じ拡張として扱われる**ようにした。
  古い版を消さずに新しい版を読み込もうとすると **Chrome が「同じ ID の拡張機能が既にあります」で拒否する** —
  2 つとも動いて**同じ相手に 2 回投稿する**のを防ぐため

### 移行時の注意

- ⚠️ **0.3.0 に上げるときだけ、設定が引き継がれない。**
  0.2.0 までは**フォルダのパスから ID が決まっていた**ため、0.3.0 は別の拡張として入る。
  **呼び名の辞書・文面・スイッチをもう一度入れ直してください。**
  引き継がれるようになるのは **0.3.0 以降の入れ替えから**

### 既知の未検証

- **コメント返しの実配信での通し確認は一部だけ。**投稿されることは 2026-08-16 のテスト配信で
  確認したが、**固定されないこと / 2 回目が出ないこと / 自己ループしないこと /
  ポップアウトを開き直しても一斉投稿しないこと**は未確認
- コメント要素が**別の要素の中身として一緒に挿入された場合に検知が届かない**可能性がある
  (テスト配信で 1 件、診断ログが 1 行も出ないコメントを観測。未解明)
- 辞書のスクリーンショット (説明ページ) は行を畳む表示になる前のもの

## 0.2.0 (未リリース)

送信元ごとの自由文 (`{msg}`) を追加した (specs/003-per-source-message)。

### 機能

- **投稿文に `{msg}` を足せる。**呼び名の辞書に相手ごとの自由文を登録すると、その一文が差し込まれる。
  未登録・空の相手では `{msg}` ごと消え、前後の空白は 1 つに畳まれる
- 200 字を超える場合の削り順を **自由文 → 表示名 → 末尾** に変更。
  自由文か表示名を削って収まる場合、URL は壊れない
- 設定画面: 辞書テーブルの自由文欄、**投稿文全体に対する残り文字数**、
  テンプレートに `{msg}` が無いのに自由文があるときの警告

### 変更

- **呼び名の辞書の保存先を `chrome.storage.sync` から `local` へ移した。**
  `sync` は 1 アイテム 8KB 上限で、自由文が加わると数十件で保存が黙って失敗するため
  (security-review S7)。**代償として端末間で同期されなくなる。**
  以前の `sync` の辞書は 1 度だけ引き継ぐ (`sync` 側は消さない)

### 既知の未検証

- **自由文入りの文面は、実配信での通し確認 (検知 → 投稿 → 固定) が未実施。**
  手動トリガー経由での投稿は確認済み
- 辞書のスクリーンショット (説明ページ) は自由文の列が増える前のもの

## 0.1.0 (未リリース)

初版。実配信で ①検知 → ②投稿 → ③固定 が通ることを確認した状態
(確認できたのは日本語 UI・Studio のチャット・1 環境だけ)。

### 機能

- ライブリダイレクトの受信通知を検知して、送信元チャンネルの URL をライブチャットへ投稿する
- 投稿したメッセージを固定する (`off` / `ifEmpty` / `always`)
- 投稿文のテンプレート、送信元ごとの呼び名の辞書
- 同一送信元のクールダウン (既定 10 分)
- 手動トリガー「↩ 返礼」。**自動投稿を切っていても実行できる** (書き込みを人の操作だけに限れる)
- 対象を `studio.youtube.com` のライブ管制室だけに限定

### 配布

- **Chrome ウェブストアには出さない。** GitHub Releases での配布にする
  (理由は [README](README.md) の「使う前に読んでください」)
- 説明ページ (GitHub Pages) と導入手順を用意
- `v*` タグの push で Release に ZIP を添付するワークフロー
- アイコンを追加、拡張の表示名を「リダイレクト返礼ピン」に
