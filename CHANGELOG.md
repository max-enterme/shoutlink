# 変更履歴

## 0.2.0 (未リリース)

送信元ごとの自由文 (`{msg}`) を追加した (specs/003-per-source-message)。

### 機能

- **投稿文に `{msg}` を足せる。**呼び名の辞書に相手ごとの自由文を登録すると、その一文が差し込まれる。
  未登録・空の相手では `{msg}` ごと消え、前後の空白は 1 つに畳まれる
- 200 字を超える場合の削り順を **自由文 → 表示名 → 末尾** に変更。
  自由文か表示名を削って収まる場合、URL は壊れない
- 設定画面: 辞書テーブルの自由文欄、**投稿文全体に対する残り文字数**、
  テンプレートに `{msg}` が無いのに自由文があるときの警告

### 変更

- **呼び名の辞書の保存先を `chrome.storage.sync` から `local` へ移した。**
  `sync` は 1 アイテム 8KB 上限で、自由文が加わると数十件で保存が黙って失敗するため
  (security-review S7)。**代償として端末間で同期されなくなる。**
  以前の `sync` の辞書は 1 度だけ引き継ぐ (`sync` 側は消さない)

### 既知の未検証

- **自由文入りの文面は、実配信での通し確認 (検知 → 投稿 → 固定) が未実施。**
  手動トリガー経由での投稿は確認済み
- 辞書のスクリーンショット (説明ページ) は自由文の列が増える前のもの

## 0.1.0 (未リリース)

初版。実配信で ①検知 → ②投稿 → ③固定 が通ることを確認した状態
(確認できたのは日本語 UI・Studio のチャット・1 環境だけ)。

### 機能

- ライブリダイレクトの受信通知を検知して、送信元チャンネルの URL をライブチャットへ投稿する
- 投稿したメッセージを固定する (`off` / `ifEmpty` / `always`)
- 投稿文のテンプレート、送信元ごとの呼び名の辞書
- 同一送信元のクールダウン (既定 10 分)
- 手動トリガー「↩ 返礼」。**自動投稿を切っていても実行できる** (書き込みを人の操作だけに限れる)
- 対象を `studio.youtube.com` のライブ管制室だけに限定

### 配布

- **Chrome ウェブストアには出さない。** GitHub Releases での配布にする
  (理由は [README](README.md) の「使う前に読んでください」)
- 説明ページ (GitHub Pages) と導入手順を用意
- `v*` タグの push で Release に ZIP を添付するワークフロー
- アイコンを追加、拡張の表示名を「リダイレクト返礼ピン」に
