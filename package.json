{
  "name": "yt-redirect-pin",
  "version": "0.1.0",
  "private": true,
  "description": "YouTube ライブリダイレクトを検知して送信元 URL をライブチャットへ投稿・固定する Chrome 拡張 (MV3)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.23.1",
    "jsdom": "^24.1.3",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
